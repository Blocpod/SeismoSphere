import {createRequire} from 'node:module';
import {readFileSync,writeFileSync} from 'node:fs';
import assert from 'node:assert/strict';
import {clickGlobeControl} from './browser-controls.mjs';
const root='http://127.0.0.1:4318',{record,source}=JSON.parse(readFileSync('artifacts/chromium-response.json','utf8')),engines=createRequire(import.meta.url)('C:/Users/blocp/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright'),reports=[];
for(const engine of ['chromium','webkit']){const browser=await engines[engine].launch({headless:true,...(engine==='chromium'?{channel:'chrome'}:{})}),page=await browser.newPage({viewport:{width:390,height:844},reducedMotion:'reduce'});try{const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(root);await page.locator('.event-row').first().waitFor({state:'attached'});await clickGlobeControl(page,'instruments-open');await page.locator(`#waveform-history option[value="${source.id}"]`).waitFor({state:'attached'});await page.locator('#waveform-history').selectOption(source.id);await page.locator(`#correction-history option[value="${record.id}"]`).waitFor({state:'attached'});
 let release;await page.route('**/api/instrument-export?id='+record.id,async route=>{const reply=await route.fetch();await new Promise(resolve=>release=resolve);await route.fulfill({response:reply});});const loading=page.waitForRequest(r=>r.url().endsWith(record.id));await page.locator('#correction-history').selectOption(record.id);await loading;await page.locator('#station-search').fill('IU.ANMO.00');await page.locator('.station-row').click();while(!release)await new Promise(resolve=>setTimeout(resolve,10));const late=page.waitForResponse(r=>r.url().endsWith(record.id));release();await late;assert.equal(await page.locator('#correction-panel').isVisible(),false);await page.unroute('**/api/instrument-export?id='+record.id);
 await page.locator('#waveform-history').selectOption(source.id);await page.locator(`#correction-history option[value="${record.id}"]`).waitFor({state:'attached'});await page.locator('#correction-history').selectOption(record.id);await page.locator('#correction-result svg').waitFor();let releaseAI;await page.route('**/api/chat',async route=>{await new Promise(resolve=>releaseAI=resolve);await route.fulfill({json:{model:'Controlled timing',answer:'LATE_CORRECTION_ANSWER'}});});const thinking=page.waitForRequest(r=>r.url().endsWith('/api/chat'));await page.locator('#correction-explain').click();await thinking;await page.getByRole('button',{name:'Close stations and waveforms'}).click();await page.locator('[data-mobile="research"]').click();await page.locator('#replay-date').fill('2010-02-27T07:02');await page.locator('#strict-replay').check();await page.locator('#load-replay').click();await page.locator('#timeline-mode').filter({hasText:'STRICT OBSERVATION REPLAY'}).waitFor();while(!releaseAI)await new Promise(resolve=>setTimeout(resolve,10));const answer=page.waitForResponse(r=>r.url().endsWith('/api/chat'));releaseAI();await answer;await clickGlobeControl(page,'instruments-open');assert.equal(await page.locator('#correction-panel').isVisible(),false);assert.equal(await page.locator('#correction-answer').count(),0);assert.deepEqual(errors,[]);reports.push({engine,lateSourceLoadWithheld:true,strictCutoffHidden:true,lateAnswerWithheld:true,errors});
 }finally{await browser.close();}}
writeFileSync('artifacts/response-lifecycle-report.json',JSON.stringify(reports,null,2));console.log(JSON.stringify(reports));
