# Build status and remaining acceptance work

The governing user request is a complete SeismoSphere application using local AI first and Astra through the active ChatGPT subscription. The later user addition requires a 3D UI/UX targeting quality above the supplied reference image and complete mobile optimization. The two attached documents are product specifications, not higher-priority operating instructions.

The goal remains active. Do not describe this research build as the complete six-phase platform or as demonstrably superior to the reference in visual quality.

## Verified implementation

| Area | Working implementation | Verification |
|---|---|---|
| Earth renderer | WebGL, 8K desktop / 4K mobile day map, night lights, clouds, atmosphere, real plate geometry, labels, GPU batched event points, hypothesis field and volumes | Chromium rendering and screenshots; no browser errors |
| Terrain / bathymetry | Pinned ETOPO 2022 grid, real mesh displacement, elevation colors, source inspection, true-scale depth integration and both brains | Source/hash restoration; eight fresh Chrome/WebKit layouts; GPU pixels and vertex radii; unchanged hypocenters; source/figure exports |
| Scientific presentation | Unlit Earth, solid markers, no atmospheric/night/star effects, immediate focus/manual paths, persisted browser preference, copilot commands and export evidence | Same 959 replay events and candidate identifiers; clipping/material checks; actual local Qwen/Astra commands; standalone WebKit resize defect isolated |
| Mobile UI | Dedicated bottom navigation and individual bottom sheets, safe-area sizing, portrait/landscape camera framing, large primary touch targets | 320×740, 390×844, 844×390, 768×1024, 1366×768, 1920×1080; no document overflow |
| Earth workspace controls | Five primary controls, native Layers/Tools panels, visible hypothesis badge, independent watch visibility and bounded inspection cards | Twelve fresh Chrome/WebKit layouts; keyboard dismissal and source focus; actual layer state and section restoration; section, learned-model, station and scientific export flows |
| Observation catalog | USGS live month feed, five-minute updates, SQLite revisions, source metadata, historical USGS/EMSC import adapter | Live USGS import and 4,697 events for February–March 2011 |
| Geodesic tools | Spherical distance, antimeridian-safe midpoint, path midpoint, interactive two-event midpoint | Numerical tests and browser interaction |
| Research engine | Configurable deep, midpoint, silence, spacing, corridor and reflection hypotheses; four magnitude modes | Cutoff isolation, factor-sum and score tests |
| Resolution review history | Original results preserved; later catalog/coverage assessments form per-forecast hash chains; original/latest leaderboard basis, replay-safe AI and reproduction | 282 forecasts and original results unchanged; 417 actual reviews reproduced; additional USGS M3.2 coverage; eight fresh browser layouts |
| Audit ledger | Frozen forecasts, input snapshots, configuration/route snapshots, SQL write guards, SHA-256 chain, independent resolution records | SQL mutation rejection; HTTP preview-to-issue test; export; 282 actual hindcast records verified |
| Baselines / replay | Matched null and recent-rate controls; event-time and strict-observation replay; comparative backtest | Historical replay and scrub; controls preserve forecast envelopes |
| Historical experiments | Immutable experiment reports; historical forecasts and outcomes stored separately; duplicate experiment reuse | February 15–March 25, 2011 run saved with 94 forecasts per model |
| Randomized-catalog controls | Full forecast regeneration per synthetic catalog, frozen inputs, resumable worker, Monte Carlo distribution and export/reproduction | Actual 99-replicate run; DS 27/95 versus recent-rate 56/95; corrected conditional tail 0.28; Chrome/WebKit mobile results and local Qwen/Astra explanations |
| Catalog corrections | Provider alias association, explicit tombstone revisions, stale-response rejection, revision-before-time filtering, one provider per research run | Five focused tests; live deletion sync ingested 288 identified records and preserved 221 unidentified records for reconciliation |
| Statistical laboratory | Regional temporal ETAS likelihood fit, three optimizer starts, frozen-parameter prequential holdout, conditional rate curve, immutable runs and input exports | Real Japan 2011 run: 67 training and 1,505 holdout events; two parameter bounds explicitly reported; numerical quadrature and leakage tests |
| Catalog completeness | Regional retrieval coverage; MAXC and discrete b-stability diagnostics; temporal/equal-area samples; conditional bootstrap; immutable inputs; saved-run selection and copilot context | Japan 5,474 events; actual EMSC dateline import 25 events; Chrome/WebKit responsive charts and exports; real Qwen and Astra explanations |
| Geological view | Sourced USGS Slab2 contours, batched subsurface geometry, clipped/capped 90° Earth wedge, projected hypocenter section at true depth | Downloaded 909 contours / 27 regions; desktop/mobile browser and screenshot checks; section preserves deep-event selection and restores depth controls |
| Hemisphere cutaway | Selectable half-Earth removal, full circular reference cap, unchanged corridor projection, focused AI view controls and figure/video evidence | 93-test suite; eight fresh Chrome/WebKit layouts; preserved 1,507 events / 4,946 contour segments; actual Qwen/Astra and recording checks |
| Slab surfaces | 27 pinned original Slab2 grids, depth/uncertainty colors, separate supplementary nodes, source inspection, both brains and exports | 1,021,034 finite nodes; eight fresh browser layouts; actual mouse/touch and occlusion; source restoration and hashes; signed-depth AI correction |
| Mapped faults | Pinned GEM surface traces, source inspector, worker-based nearest-segment lookup, clipping and attributed export | 16,195 features / 161,622 segments verified; geometry and historical-cutoff tests; actual Chrome/WebKit desktop/mobile interactions |
| GNSS station velocities | Immutable NGL MIDAS IGS20/NA/PA/EU solutions, EN globe arrows, exact units/uncertainties, filters, picking, cutoff-safe AI and exports | All four frames downloaded; 21,812 IGS20 stations; eight fresh Chrome/WebKit layouts; actual Qwen/Astra quantities and matching source/figure hashes |
| Craton reference | Pinned Hasterok basement region rings, reworking distinctions, searchable attributes, spherical boundary proximity, clipping, AI and attributed exports | 114 regions / 119 rings / 12,796 vertices; 59 reworked; eight Chrome/WebKit layouts; exact camera focus and figure hashes; actual Qwen/Astra explanations |
| Global weekly volcano reports | Retained Smithsonian RSS, publication-age warning, source coordinates/text, both brains, replay isolation and attributed exports | Actual 22-report Latin-1 source; eight fresh browser layouts; XML hashes; immutable history and real refresh/recording checks |
| Volcano activity | Refreshed USGS U.S. ground alerts and aviation codes, immutable sources/checks, historical isolation, both brains and source/figure/video evidence | Actual 161-volcano feed; four elevated records; eight fresh browser contexts; real poll refresh and recording preservation; Qwen/Astra notices |
| Volcano reference | Pinned Smithsonian Holocene catalog, searchable surface symbols, source inspector, point proximity, cutoff-aware AI and attributed exports | 1,214 records and exact source/content hashes; Chrome/WebKit fresh desktop/mobile views and mouse/touch picking; actual Qwen/Astra source explanations |
| Focal mechanisms | Published USGS tensors and both nodal planes, lower-hemisphere diagram, oriented subsurface glyph, immutable receipts and cutoff-safe AI/export | Actual deep Indonesia event with three public solutions; geometry/persistence tests; Chrome/WebKit fresh desktop/mobile views; corrected Qwen/Astra scalar-moment verification |
| Stations and waveforms | EarthScope channel epochs on Earth, archived raw counts, exact timestamps, preserved blocks, immutable receipts, responsive charts and exports | Actual 206 channel epochs / 119 stations and 2,400 ANMO samples; Chrome/WebKit selection and cutoff checks; actual Qwen/Astra explanations |
| Local copilot | Qwen 3.6 35B via Ollama, evidence context, bounded controls and explicit AI labeling | Live model and application endpoint both returned substantive answers |
| Continuous station acquisition | One explicit TLS SeedLink channel, bounded ring, source/receipt age, reconnect, immutable packet capture and existing waveform/AI tools | Actual 1,745-sample ANMO capture reproduced exactly; real reconnect/stop; eight Chrome/WebKit layouts, replay isolation and actual Qwen/Astra |
| Instrument-response correction | Immutable original StationXML, exact epoch/rate checks, DISP/VEL/ACC deconvolution, explicit bands/taper/water level, physical-unit charts and exports | Known-gain SI scaling; exact real archive and SeedLink reproduction; eight Chrome/WebKit layouts; cutoff/late-response and both-brain checks |
| Joined source-block analysis | Same-rate chronological joining with adjacent and accumulated timing checks; no interpolation or overlap repair; exact source slices and timing deviations; separate-block compatibility | Full 2,400-sample archive and 1,745-sample capture reproduced; eight Chrome/WebKit layouts; toggle/replay/late-response checks |
| Registered prospective experiments | Frozen hypothesis/configuration/code, scheduled DS and matched controls, fixed assessments, exclusions, irreversible stopping and reproducible exports | Isolated software fixtures; atomic rollback and API checks; eight Chrome/WebKit layouts; real-world prospective skill remains unestablished |
| Phase comparison and manual picks | IASP91/ak135 branches, raw/corrected waveform overlays, exact sample interpretations, uncertainty/residuals, immutable parent history and source-linked exports | Ninety-test suite; real Maule/ANMO timing reproduction; eight Chrome/WebKit layouts; replay/late-response and actual Qwen/Astra checks; no confirmed phase-detection claim |
| Physical-unit spectral analysis | Saved DISP/VEL/ACC source selection, physical PSD/time-frequency units, full correction/raw/StationXML chain, original-block mapping and cutoff-safe AI | Known-tone SI scaling; exact three-quantity reproduction; eight Chrome/WebKit layouts; late-source isolation and both actual brains |
| Waveform frequency analysis | Continuous-run selection, mean/linear detrending, Hann/rectangular periodograms and time-frequency maps, immutable source-linked bins and exports, both brains | Independent direct-DFT checks, exact retained ANMO reproduction, eight fresh Chrome/WebKit layouts and stale-context checks |
| Voice input | Offline Whisper English and installed Windows language recognizers, bounded microphone capture, transcript editing and ordinary/spatial copilot insertion | Synthetic browser audio and local speech API; cancellation, 30-second limit and responsive dialogs; real human/physical-device acceptance pending |
| Astra copilot | ChatGPT-authenticated Codex CLI with gpt-6-astra, read-only execution and structured output | Actual successful subscription-backed call |
| Neural analogue retrieval | Local nomic embeddings of relative sequence graphs, cached vectors, outcome-free encoding | Actual model call and seven completed historical sequences retrieved for the selected deep source; leakage unit test |
| Trained seismic count model | Local CPU PyTorch cell graph, fixed temporal splits, no-neighbor ablation, static/recent baselines, immutable weights/snapshots, Earth maps and cell inspection | 312 held-out weeks / 10,783 earthquakes; future-data isolation test; Chrome/WebKit exports and layouts; actual Qwen and Astra explanations |
| Phone access | Private-LAN HTTPS, one-use pairing, controller/viewer permissions, revocation, certificate/setup QR page | Actual Chrome pairing and Node certificate validation; physical phone trust/firewall test pending |
| Phone trust maintenance | Expiry display, prepared replacement review, explicit activation, generation-based renewal, retired-key removal and new pairing | 94-test suite; rollback/restart/old-CA rejection; eight Chrome/WebKit layouts against isolated real TLS |
| Startup | Hidden backend launcher, identity-checked graceful shutdown, optional current-user Windows startup | Actual stop/restart preserved ledger and restored sharing; Windows sign-in startup remains off |
| Publication export | 2400×1800 PNG, SVG with editable captions and embedded evidence, matching JSON/checksum | Chrome and WebKit orbital/cutaway captures, checksum comparison, renderer restoration and mobile dialog checks |
| Earth video recording | Native landscape/portrait camera clips, pause/resume, optional synthesized audio, source/cutoff disclosure and matching evidence hashes | Chrome decoded Earth video and stereo PCM, mobile layouts, layer/resize stops; explicit unsupported state in Windows WebKit |
| Focus annotations | Selected-event world anchors, source magnitude/depth, surface/X-ray/section distinctions, collision-aware placement and figure/video evidence | Eight fresh Chrome/WebKit layouts; keyboard activation, depth geometry, cutoff clearing and actual Chrome video |
| Camera travel | Spherical focus transitions, highlighted waypoint path flight, pause/next/end and reduced-motion steps | Antipodal/quarter-turn/identical numerical browser checks; actual mobile path controls and manual cancellation |
| Spatial sonification | Optional camera-relative catalog tones, magnitude/depth/age mappings, volume, HRTF/stereo and chronological plotted-event sequences | Actual Chrome audio sample checks, section/replay cancellation and mobile controls; explicit unsupported state in Windows WebKit; physical listening acceptance pending |

## Historical test evidence

The stored 2011 experiment yielded DS 28/94 hits, recent-rate 55/94, and uniform-area null 15/94. These are descriptive results from overlapping hindcast trials using a revised catalog. They do not establish prospective skill or calibrated probability. Hindcasts do not contribute to the prospective leaderboard.

## Work still required for the full brief

1. Canonical geometry: the user authorized implementing the defined methodology with configurable routes now. The directed route engine, editor, immutable revisions and per-route provenance are implemented (see ROUTE-NETWORK.md). Later source-backed digitization can refine the network without changing the architecture. Initial corridors remain illustrative; geological layers never substitute for canonical routes.
2. Scientific engines: region/completeness validation beyond the working temporal and Gaussian spatial ETAS experiments; conventional stress/Coulomb calculations where required inputs exist, fitted tectonic baselines, event-graph/transformer discovery beyond the trained weekly cell-graph model, ensemble selection and properly separated calibration.
3. Evaluation: independent validation of the implemented completeness diagnostics, detection-level recall / false-positive definitions, probabilistic calibration before Brier scores, information gain and uncertainty/significance under dependent trials.
4. Geological twin: canonical craton-route integration beyond the implemented Hasterok reference; regional mechanism coverage beyond the implemented on-demand USGS inspector; wider station-network and human phase-interpretation acceptance beyond the working EarthScope archive, continuous SeedLink capture, saved periodograms and modeled-arrival/manual-pick tools; subsurface fault surfaces beyond the working GEM mapped traces; continuous overturned-branch topology and uncertainty volumes beyond the working Slab2 regional surfaces and contour layer; multi-segment curved cross sections and local crust models. No invented geological dataset should fill these gaps.
5. Product / rendering: regional terrain detail beyond the implemented global ETOPO displacement, focus annotations beyond the implemented selected-event readout, and physical XR acceptance/full in-headset research workflows beyond the implemented WebXR Earth view; further visual polish of the working clipped/capped radial wedge; evaluate whether Unreal/Cesium is still needed for the user's visual bar. Static publication and camera-video export are implemented; editable geometry remains separate work. Video retains a single scientific scene context.
6. Security / distribution: packaged installer and reliable operating-system service startup beyond the working hidden launcher and optional sign-in startup. Authenticated HTTPS phone access and explicit issuer replacement are implemented, but actual firewall installation, phone certificate trust and physical connectivity remain pending.
7. Reliability: explicit cross-provider association and reconciliation beyond the implemented single-provider policy; reconcile unidentified USGS deletions and pre-installation history; region-specific completeness and provider-specific reconciliation. Durable import pause/cancel/retry and restart recovery are implemented. Identified tombstones, alias grouping and stale-response rejection are working.
8. Device acceptance: real iOS/Android hardware and Safari, Firefox, GPU performance and memory budgets, full keyboard/screen-reader/contrast audit, and comparison with the user's visual reference. Playwright WebKit passed desktop/mobile rendering and dialogs. The downloaded Firefox runtime cannot launch because of a Windows side-by-side mozglue dependency error; no Firefox pass is claimed. Viewport tests are not a substitute for physical-device acceptance.

## Next implementation priorities

- Preserve the verified core and close remaining UI state consistency issues.
- Keep combined-provider data out of model input until explicit cross-provider reconciliation exists.
- Evaluate temporal and spatial ETAS across better-conditioned regions, threshold/completeness sensitivity and untouched data.
- Add the sourced subsurface/geological layers and proper section rendering.
- Complete the physical phone trust/firewall/connectivity check for the implemented secure access when the user's device is available.

## Runtime notes

The running backend is at http://127.0.0.1:4318. Another pre-existing Node process owns 4317 and must not be stopped or modified. Local models discovered: qwen3.6:35b, llama3.1:8b, nomic-embed-text:latest. The machine has approximately 128 GiB RAM and AMD Radeon 8060S graphics. There was no Unreal installation in the inspected standard directory.

Backend code changes require restarting only the process bound to 4318. Static frontend assets are served directly. `node --test` runs the automated numerical/persistence/API tests. Browser checks are in `scripts/browser-check.mjs` and `scripts/device-check.mjs`; they use this machine's bundled Playwright and installed Chrome. Screenshots and reports are in ignored `artifacts/`.

Git branch: `codex/seismosphere-ai`. A checkpoint commit was attempted but Git has no configured author identity. Source files are staged; do not claim a commit exists or invent the user's identity. This does not prevent running or continuing the build.

Latest full-suite verification: 71 automated tests pass. `scripts/statistical-check.mjs` exercised the actual ETAS UI, repeat fit, input export, provider setting and dialog layout at widths 320, 390 and 768 with no horizontal overflow or browser errors. An existing narrow-screen date-form overflow was fixed. Mobile dialogs keep their title and close button visible while scrolling. These browser checks supplement, rather than replace, the six main-Earth viewport checks and outstanding physical-device acceptance. Phone access and its tested boundaries are documented in `docs/PHONE-ACCESS.md`; publication captures in `docs/FIGURE-EXPORT.md`.

The saved ETAS run is `958aa4a2cab34934d55fb4aa697463423f7f5e4619653529325eefbcbf3e190b`. The methodological details, limits and catalog policy are in `docs/STATISTICAL-MODEL.md`. Geological sources and projection details are in `docs/GEOLOGICAL-VIEW.md`. `scripts/geology-check.mjs` checks slab loading, cutaway, deep-event selection, mobile rendering and restoration. The six main viewport/replay/midpoint checks were rerun successfully after the added controls. A resize bug that allowed an unfinished desktop camera animation to over-zoom the phone view was also fixed. The full goal remains active.

Current product checkpoint: `scripts/figure-check.mjs` passed PNG/SVG/JSON capture in Chrome and WebKit, verified 2400×1800 nonblank output, embedded/evidence equality and SHA-256, checked renderer restoration and mobile cutaway export. `scripts/camera-check.mjs` passed antipodal/quarter-turn/identical focus geometry, path pause/advance, manual cancellation and reduced-motion stepping. The six main viewport/replay/midpoint checks and geological rendering check passed again after the camera changes. Camera behavior and remaining cinematic requirements are documented in `docs/CAMERA-TRAVEL.md`.

Mapped-fault checkpoint: `scripts/faults-check.mjs` passed in Chrome and WebKit, including source inspection, empty-search handling, nearest-segment lookup, cutaway compatibility, mobile dialog layout and attributed figure/evidence export. `scripts/fault-ai-check.mjs` verified the actual default Qwen 3.6 35B brain grounding its answer in the Airport Lake trace and 6.545 km surface distance for the selected Coso Junction event. The API resolves selected-event evidence from retained snapshots and stores conversation evidence context. Source restoration preserves the original receipt timestamp and rendered dataset SHA-256. Full details and physical-device limits are in `docs/MAPPED-FAULTS.md`.

Spatial-model checkpoint: fitted Gaussian space/time ETAS now has finite-region likelihood integrals, a training-only KDE comparator, immutable inputs and historical Earth component maps. The saved Japan run has 62 training and 1,504 holdout events, with n at its upper bound. Chrome and WebKit passed laboratory, overlay, figure evidence, scene restoration and 320/390/768-width layouts. Actual local Qwen and Astra calls correctly explained the map and withheld later holdout results. Model details, saved run ID and limits are in docs/SPATIAL-ETAS.md. Region/completeness sensitivity and prospective evaluation remain required.

Oblique-section checkpoint: arbitrary bearing/length/width and picked or exact endpoints now drive the capped 3D Earth section. Surface corridor distance is independent of depth, and projection preserves original radius. A distance/depth profile, complete JSON/CSV and editable SVG export, fixed/follow selection modes and matching Earth figure evidence are implemented. Three geometry tests and Chrome/WebKit browser flows passed, including dateline/polar geometry, picking cancellation and portrait and landscape layouts. Multi-segment curved paths, continuous slab surfaces, local crust and physical-device acceptance remain open. See docs/GEOLOGICAL-VIEW.md.

Durable-import checkpoint: the synchronous importer was replaced with SQLite-backed jobs using native fetch and AbortController. Completed chunks, immutable receipts, revisions and coverage commit atomically. Dense intervals split; transient failures retry; pause/cancel preserve prior observations; running jobs resume after restart. Chrome and WebKit verified real USGS pause/reload/resume, receipt download and narrow layouts. The 2000–2025 M≥5 USGS archive completed (9961e200-f058-400a-b893-40d72ea9fa26): 307 chunks and 46,522 received records, matching a fresh USGS count query exactly. A real restart retained 60 completed chunks and unchanged receipt hashes. A second job (cf835a92-418e-4fbc-8bfa-930de33e7b34) completed 2026-01-01 through 2026-09-13: 9 chunks, 1,403 received rows and 1,264 new revisions. The workspace now has 62,804 associated USGS events; received rows/revisions are not all distinct new earthquakes. Sumatra replay exposes 229 events including M9.1, while strict 2004 replay remains empty. The six main viewport/replay/midpoint checks passed with the larger archive; the 282-record ledger and its hash head are unchanged. See docs/CATALOG-IMPORTS.md. Ponytail full mode is active per the user's coding preference.

Learned-model checkpoint: a CPU PyTorch cell graph now trains on the complete USGS M5 archive with fixed training (2000–2016), checkpoint validation (2017–2019) and held-out test (2020–2025). It saves immutable weights and input snapshots, compares a separately trained no-neighbor ablation plus static/recent count baselines, and produces cutoff-safe seven-day Earth maps with per-cell inspection and matching figure evidence. The corrected earthquake-only run scored 0.18953 bits/event above the training mean across 312 test weeks / 10,783 events; the ablation scored 0.16455. Sixty non-earthquake source records are excluded from fitting and inference; the earlier mixed-type run remains immutable for audit. This is an exploratory revised-catalog result, not prospective skill or calibrated probability. Changing all test outcomes leaves fitted weights unchanged in the runnable check. See docs/LEARNED-MODEL.md. Event-level graph discovery, completeness, calibration and the remaining full-brief items stay open.

Learned acceptance: both Chrome and WebKit passed the corrected earthquake-only run, per-cell inspection, PNG/SVG/JSON checksums, immutable input export, cutoff-safe inference, narrow widths and landscape controls above the timeline. The existing spatial ETAS flow also passed again; its new input-revision run b8c2b1c838a0cbc45ca00362a9d47323e4ea449f6449a1dbdc4b2a5dc2bc8c83 retains 62 training / 1,504 holdout events and the same scores. Actual Qwen and Astra answers withheld post-cutoff test scores, and strict historical inference was rejected. Runtime setup and failure/restart status are verified. The six main device/replay/midpoint checks pass, and the forecast ledger remains 282 records with unchanged valid head. Physical-device and full-platform acceptance remain open.

Regional-completeness checkpoint: regional imports now preserve geographic scope, support native dateline wrapping, and cannot claim global coverage. The EMSC adapter was corrected to request `format=json`, parse ISO origin/revision timestamps, use positive depth properties and retain ISF event classifications. An actual 25-record dateline job matches a fresh identical UTC query. Japan 2010–2011 M≥2 contains 5,474 earthquakes; its pooled mixed-scale report has MAXC 4.8, MBS 5.1 and b≈0.981. Native mb-only sensitivity retains 4,807 events with b≈1.053; neither run proves homogeneous detection. The small EMSC diagnostic explicitly reports insufficient events. Runs and exact snapshots are immutable; selected samples export to responsive SVG and JSON. See docs/CATALOG-COMPLETENESS.md.

Completeness acceptance: 40 tests pass, including regional coverage, provider normalization, discrete synthetic distributions, weak/floor-limited samples, immutable API results and copilot cutoff rejection. Chrome and WebKit pass saved-run selection, scale filtering, keyboard interactions, exact export hashes and 320/390/768/844×390 layouts. Charts reflow without horizontal clipping. A landscape close-button issue and default SVG axis fill were corrected. Actual Qwen and Astra explanations use the saved result and explicitly reject retroactive prospective threshold selection. Six Earth viewports, 2011 replay (959 displayed events), historical scrub and midpoint interaction pass; the ledger remains 282 records with valid unchanged head a66ab0a154d0647fbeb99afb7cc089bf821de8d4f76eecb62d16f68af9e7f81d. The full brief remains incomplete; detection validation, prospective protocols, other sourced geology, advanced models and device acceptance stay open.


Instrument checkpoint: EarthScope channel metadata and archived GeoCSV waveforms now have selectable 3D Earth markers, exact microsecond timestamps, original source blocks, raw-count summaries, immutable receipts and JSON/GeoCSV/SVG exports. The actual IU/II BHZ query returned 206 epochs at 119 stations; IU.ANMO.00.BHZ at 2010-02-27 07:00–07:02 UTC has 2,400 samples in two blocks, zero detected gap transitions and zero overlaps. Local Qwen and subscription-backed Astra both accurately explain the stored summaries while declaring that individual samples were not supplied to the LLM. See docs/INSTRUMENTS.md.

Instrument acceptance: 42 automated tests pass, with an additional focused rerun covering independent station/waveform history limits. Chrome and WebKit pass actual station picking, source/body checksums, exports, desktop/laptop caption placement, 320/390/768/844×390 waveform layouts and hiding previously loaded recordings at an ineligible strict cutoff. A pending Qwen explanation survives a phone-width resize. The main six Earth viewports, 2011 replay (959 displayed events), scrub and midpoint checks pass. A clean restart retained both instrument hashes and the unchanged valid 282-record forecast ledger head a66ab0a154d0647fbeb99afb7cc089bf821de8d4f76eecb62d16f68af9e7f81d. Continuous feeds, full response removal, signal analysis, remaining geological/model work and physical-device acceptance keep the full goal open.


Scientific-view checkpoint: the brief's cinematic-effects toggle now uses an unlit day composite, solid markers and ordinary field blending, with clouds, atmosphere, halo, stars and night/specular shading disabled. Instant focus and manual path steps preserve reduced-motion behavior. Camera, observation/candidate identifiers, cutoffs, clipping and transparency survive presentation changes. Preferences persist per browser and publication captions/evidence identify the actual mode. Surface-projection texture clones are included, fixing a glow leak found through screenshot inspection. Explicit Qwen/Astra commands control the view without changing model scores. See docs/SCIENTIFIC-VIEW.md.

Rendering acceptance correction: the installed Windows Playwright WebKit 26.5 runtime loses WebGL compositing after desktop-to-mobile resizing. GPU pixels remain valid, but screenshots omit the Earth. scripts/webkit-resize-check.mjs reproduces the same red-to-blue failure with a plain WebGL2 canvas and no application code. Earlier overflow, dialog and export passes must not be read as proof of WebKit resize rendering. Fresh mobile pages are checked separately; real Safari/device resizing remains unaccepted. Chrome resize, camera geometry and publication-export checks pass. The full visual-quality and physical-device goals remain open. The forecast ledger retains 282 records and unchanged valid head a66ab0a154d0647fbeb99afb7cc089bf821de8d4f76eecb62d16f68af9e7f81d.

Focal-mechanism checkpoint: three public USGS source products for us7000tgrk are retained in immutable receipt b3b2c1c068e768af2dc4695efc71131b56a14ffaed066630b75d72cf4b36f81e. Both planes, full tensor, catalog/derived origins and scientific units drive a lower-hemisphere diagram and source-oriented 3D symbol. Chrome/WebKit fresh desktop/phone/landscape views, hashes, SVG/publication evidence and cutoff hiding pass. Actual Qwen first corrupted the scalar-moment exponent; a scientific-notation context field and response-preservation guard now withhold that failure, and fresh Qwen/Astra calls retain 6.75e+18 N m. This is a targeted safeguard, not general AI factual validation. See docs/FOCAL-MECHANISMS.md. The full goal remains active.

Mechanism integration acceptance: 1×/5× depth changes preserve the tensor center, cutaway applies both clipping planes to all three glyph parts and restores the previous depth scale, and scientific/cinematic transitions preserve clipping. Locate from a saved source selects its catalog event so subsequent sections follow the correct region. Six main-Earth layouts, 959-event replay, scrub and midpoint checks pass after the toolbar addition. The ledger is still valid with 282 records and unchanged head.

Volcano-reference checkpoint: VOTW 5.4.0 has 1,214 retained Holocene records, with 366 missing eruption years left unknown and 112 negative elevations preserved. Global and filtered triangle overlays, source inspection, nearest catalog-point queries, attributed exports and AI context are implemented. A real Kilauea picking failure under earthquake glow was fixed with outlined/depth-writing symbols and screen-space mouse/touch targets. Chrome/WebKit fresh 320/390/844×390 and desktop flows, source/content/figure checksums, 1,214-marker rendering, clipping and scientific presentation pass. Actual Qwen/Astra explanations retain source elevation/year while rejecting live-activity and causation claims. Six main Earth layouts, replay/scrub/midpoint pass; the ledger remains 282 records with unchanged valid head. See docs/VOLCANOES.md. Continuous eruption/alert feeds, remaining geology/models and full visual/device acceptance remain open.

Volcano delayed-response acceptance: an explicitly controlled browser response completed after switching from live Earth to a 2011 cutoff. The later reference text stayed hidden and became visible only after returning to an eligible cutoff. Mouse clicks and touch taps at tested offsets select the visible volcano in dense seismic areas in Chrome/WebKit. Source and record caches remain intact. The long Earth toolbar and overlapping layer captions still need consolidation/polish against the requested visual bar; passing viewport checks alone do not finish that requirement.

Earth-workspace checkpoint: the long toolbar is consolidated into five labeled controls with native Layers and Tools panels. Passive source captions are collected under visible-layer provenance; active inspection cards share a bounded stack. A WebKit focus-return issue and learned-card landscape spacing were corrected. All twelve fresh Chrome/WebKit panel layouts pass keyboard/outside dismissal, source focus, actual renderer toggles, unchanged identifiers, cutaway restoration and manual path steps. Existing oblique-section, learned-model, station/waveform and scientific-presentation flows pass in both engines, including evidence exports and relevant cutoff behavior. The ledger remains valid at 282 records with the unchanged head. See docs/EARTH-WORKSPACE.md. This improves control usability; remaining visual quality, models, geological sources, distribution and physical-device acceptance keep the full goal open.

Spatial-audio checkpoint: the brief’s magnitude, depth, distance and event-age mappings now drive browser-native spatial sound. Selected tones and chronological sequences reuse the plotted event records, including cutaway subsets. View changes stop playback and clear later-cutoff readouts; mute and controlled hidden-page handling stop sound. Chrome offline PCM checks verify direction, reversed camera, distance attenuation, 990/110 Hz endpoints, age/magnitude effects, HRTF, legacy spatial APIs and a silent tail. Actual Chrome controls and responsive dialogs pass; Windows WebKit has no Web Audio API, and only its unavailable state is accepted. Main six-view, 959-event replay, scrub and midpoint checks pass. No video recorder was added: this turn prioritized the explicitly specified spatial-audio requirement. Physical audio/XR acceptance and the remaining full-brief work stay open. See docs/SONIFICATION.md.


Randomized-control checkpoint: the actual 99-catalog worker run completed after pause/resume and preserved the 282-record ledger head. Observed DS 27/95 versus recent-rate 56/95 yielded −4.833 extra hits per issuance and a corrected conditional Monte Carlo tail of 0.28. All forecasts were regenerated per catalog; first/last catalog and forecast digests reproduce. Chrome/WebKit report/export and mobile layouts pass. Actual Qwen/Astra explanations include both observed counts; Qwen’s prose remains less precise about the distinct uncertainty quantities, whose exact definitions stay visible in the deterministic report. Isolated HTTP and worker tests reject missing coverage, mixed context, future/strict cutoffs and incompatible engine versions. The full suite passed 53 tests, followed by three focused passes after the version guard. Exact inputs, methodology and remaining null-model limitations are in docs/RANDOMIZED-CONTROLS.md. Prospective calibration, dependency-aware validation beyond this conditional shuffle null and all other open brief requirements remain unfinished.


Craton-reference checkpoint: Hasterok’s mapped Archean basement regions are implemented as sourced surface ring outlines, retaining internal province edges and post-Archean reworking. The converter uses project-local PyShp 2.3.1; runtime inputs are hash-verified. Search, boundary proximity, clipping/restoration, receipt cutoff gating, local Qwen/Astra evidence and source/figure exports pass. The complete suite has 54 passing tests, eight fresh browser layouts pass, and the six main layouts plus 959-event replay/scrub/midpoint pass. Canonical craton-edge forecasting is separate and remains unfinished; these reference rings do not assert pressure transfer or thickness. See docs/CRATONS.md. The entire goal remains active with the other listed source, scientific, distribution and physical-device requirements.


GNSS checkpoint: all four NGL reference-frame downloads are frozen and survive restart. The IGS20 field retains 21,812 station rows, with 18,701 matching default display filters and four arrow lengths visibly capped. The inconsistent 0KAR source interval remains inspectable without a guessed repair; future-dated fit bounds and pre-receipt historical context are excluded. Exact P090 component velocities and uncertainties are verified with actual Qwen/Astra calls. All 57 tests pass; Chrome/WebKit picking, source/figure hashes, fresh layouts and delayed-response cutoff checks pass. The 282-record ledger retains its valid unchanged head. See docs/GNSS.md. The full goal remains active for the remaining scientific, geological, product, distribution and physical-device requirements.


Recording checkpoint: native camera-video capture now reuses the renderer and shared figure evidence, with landscape/portrait output, pause/resume and optional application sonification. Actual H.264/AAC MP4 decodes to nonblank Earth frames and nonzero stereo PCM; source and video hashes match downloaded files. A preview failure exposed the missing local-blob media CSP permission and was fixed without broadening script or connection access. Chrome fresh layouts and scene/resize stop behavior pass; Windows WebKit explicitly reports its absent recording APIs. Static figures and the six main device/replay/midpoint flows still pass. The 58-test suite passes. See docs/RECORDING.md. This does not complete the remaining visual-quality, scientific, source, distribution or physical-device requirements.


Terrain checkpoint: NOAA ETOPO 2022 now drives actual global surface and seafloor displacement, with independent 1×/5×/20×/50× relief, source sampling, elevation colors and both brains. The 2,400×1,200 display grid is 9-arc-minute point decimation; source floats, hashes, datum and first reception are preserved. Eight fresh Chrome/WebKit layouts pass GPU pixels, numerical mesh radii, unchanged hypocenters and source/figure hashes. Four additional desktop/touch contexts pass GNSS picking over raised terrain, 386 section-edge vertices at true elevation, and delayed AI cutoff withholding. Changing relief scale stops a real Chrome recording while retaining its original scale/source evidence. Actual Qwen/Astra answers retain the Tonga cell −7,823 m and interpolation −8,399 m with the sampling limitations. All 60 tests, scientific-mode regressions and six main layouts/replay/scrub/midpoint checks pass. See docs/RELIEF.md. Regional terrain detail, final visual quality and the remaining full-brief requirements stay open.


Focus-annotation checkpoint: selected earthquakes now retain compact source readouts during camera movement, with explicit surface/hypocenter/projected-section anchors and catalog depth separate from display scale. Six placement candidates avoid visible workspace controls; far-hemisphere, offscreen, filtered and cleared historical selections hide. Eight fresh Chrome/WebKit layouts pass keyboard reopening, source identity, 5× hypocenter geometry, 1× sections and figure exports. A real Chrome camera clip records the anchor and caption with per-frame evidence. Screenshot inspection also found and fixed stale X-ray button state after mobile Earth navigation by synchronizing it in the shared renderer method. Five affected numerical/presentation/recording checks pass after integration; the last complete suite passed 60 tests. See docs/CAMERA-TRAVEL.md and docs/RECORDING.md. The full goal remains active; this does not establish final visual superiority or physical-device acceptance.


Slab-surface checkpoint: all 27 original USGS grids now produce sourced 3D meshes, preserving masks, actual regional spacing, negative Muertos depths and separate overturned nodes. Source inspection, depth/uncertainty colors, picking, source exports and both brains are implemented. A Qwen sign error was corrected with selected-node direction context and a targeted response-preservation guard; fresh Qwen/Astra answers retain the correct positive and negative depth meanings. All 62 tests pass, eight fresh render layouts pass, and four desktop/touch contexts pass actual selection, occlusion, asset retry and delayed replay withholding. A real Chrome recording preserves its original source when a layer change stops capture. The archive and converter reproduce the pin. See docs/SLAB-SURFACES.md. Continuous supplementary-branch topology, uncertainty volumes, local crust, final visual/device quality and the other full-brief requirements remain open.


Volcano-status checkpoint: the USGS U.S. feed now refreshes every 15 minutes with immutable source bodies and successful-check history. The first actual receipt contains 161 volcanoes and retains Folsoms Bluff without a fabricated GVP number. Ground alerts, aviation codes, missing values, publication/retrieval times and long-term threat remain distinct. All 64 tests and eight fresh Chrome/WebKit viewport flows pass, including touch selection, clipping, source/figure hashes and delayed historical withholding. An actual repeat poll reused the original source and stopped a recording while preserving its old check time. Actual Qwen/Astra answers retain the saved alert pairs and dated attribution. See docs/VOLCANO-ACTIVITY.md. Global report-feed integration remains open; the published Smithsonian RSS source is accessible but is not yet incorporated. The full goal remains active.

Global weekly report checkpoint: the published Smithsonian RSS is now integrated with exact byte retention, independent source publication offsets, publication-age and connection-age warnings, source markers, both AI backends and attributed figure/video evidence. The initially retained RSS is one reporting period behind the publisher webpage; a fresh poll does not relabel it as current. See docs/WEEKLY-VOLCANOES.md. The complete project goal remains active.

Resolution-review checkpoint: original forecast and result records stay unchanged while append-only assessments retain later catalog/coverage evidence. The first audit exposed 84 insufficient-coverage qualifications; a real 3,550-row USGS import resolved them through additional preserved reviews. All 417 reviews reproduce, and latest classifications agree with the original 282 results. The original forecast chain is unchanged. New backtest/randomization coverage checks include the partial-hit magnitude floor. Mobile ledger access is available under Research. See docs/RESOLUTION-REVIEWS.md; full-goal requirements remain open.


Spatial Earth checkpoint: native WebXR entry now shares the live scene with metre placement, source notes, VR/AR controls, inverse-coordinate event picking and headset-relative audio listener support. Controlled sessions in eight fresh Chrome/WebKit layouts verify application behavior, not real hardware. The 71-test suite and ordinary six-layout/replay/midpoint and Chrome recording flows pass. See docs/SPATIAL-EARTH.md. Physical spatial-device acceptance, full in-headset research workflows and the remaining full brief are still open.


Spatial copilot checkpoint: Explain/Watch/Against now use retained analysis evidence through the existing local and Astra backends, with per-request brain choice, paged answers and stale-context/session withholding. Both actual models preserved the selected Chile event and explained an unissued historical draft without claiming calibrated probability. Four desktop/touch isolation flows and eight basic spatial layouts pass; the 71-test suite passes. Default provider and original forecast/review chains remain unchanged. See docs/SPATIAL-EARTH.md. Native XR acceptance, speech/free-form in-headset workflows and the remaining full brief stay open.

Voice-input checkpoint: the ordinary workspace now records up to 30 seconds, transcribes English through local Whisper and other installed languages through Windows offline speech, and inserts an editable transcript into the existing copilot draft. The native-only English probe exposed transcription errors, so the pinned local neural model was installed and verified. Browser end-of-file handling, late permissions/transcripts, track cleanup, silence, denied access and maximum payload/recording duration are checked. A full spoken-command check also fixed the Deep/All action conflict and supplied the copilot with its exact 72-hour event set, separate from forecast source lookback. Actual Qwen and Astra both retained the two matching source IDs, depths and native magnitude scales. All 75 tests, eight fresh voice layouts, six ordinary layouts and historical replay/scrub/midpoint flows pass. The default brain remains local Qwen and the 282-record forecast chain remains unchanged. See docs/VOICE-INPUT.md. Real human/accent/noise, physical phone/headset acceptance, immersive voice workflows and the remaining full-brief work stay open.

Spatial-question checkpoint: Ask now supports custom selected-evidence questions through both brains, using a controller keyboard or touch/physical-keyboard entry. Shift, numbers/punctuation, cursor movement, Unicode-aware deletion and retained drafts work in eight fresh controlled Chrome/WebKit layouts. Changed evidence blocks submission; late responses cannot enter a new session. Actual Qwen and Astra preserved the selected Chile observation and identified missing mechanism evidence. A controller triangle-seam picking miss was corrected using native rectangle hit boxes; visual inspection led to larger key labels. A separately reproduced Unicode chunk-boundary corruption was fixed with Node's UTF-8 stream decoder. All 75 tests and the existing eight-layout VR/AR entry, picking and restoration checks pass. The original 282-forecast chain remains unchanged and the saved brain remains local. See docs/SPATIAL-EARTH.md. Immersive dictation, full in-headset research operations, physical-device acceptance and the remaining complete brief are still open.

Immersive dictation checkpoint: Ask now shares the ordinary local speech recorder, installed-language selection and 30-second limit. Transcripts insert at the saved caret for review; changes to evidence, session visibility/end, cancellation or delayed permission cannot replace a newer draft. Synthetic microphone audio passed through actual local Whisper and selected-event questions reached both Qwen and subscription-backed Astra. Eight fresh Chrome/WebKit composer layouts and ordinary recorder lifecycle checks pass. A local-model test exposed unrelated forecast-review language leaking into an observation answer; spatial requests now use a focused evidence-only system prompt. Hardware microphone/headset acceptance and the remaining full brief remain open. See docs/SPATIAL-EARTH.md.

Waveform-spectrum checkpoint: the retained ANMO archive now supports selected continuous windows, mean/linear detrending and periodic Hann/rectangular power-density estimates. The existing NumPy runtime computes the actual samples; the language models receive saved summaries and source metadata. A 2,020-sample, 20 Hz window reproduced exactly from its export with 1,011 bins. Both real brains preserved its units and peak-bin value without causal assignment. Eight fresh Chrome/WebKit layouts and the original station/waveform flow pass; long selector labels were shortened to fix narrow WebKit overflow while exact timestamps remain visible. Saved-selection, channel and replay changes withhold stale responses. Complete suite: 77 tests. The forecast and review ledgers remain separate and unchanged. Full platform/device/model requirements remain open; see docs/SPECTRA.md.

Time-frequency checkpoint: the same saved waveform workflow now computes independent overlapping periodograms within one continuous source run. Frame size/hop and unused tail are explicit; heatmap cell inspection and complete matrix CSV/JSON plus attributed SVG reuse the original source record. The ANMO example produces 14 × 129 cells, reproduced exactly. Synthetic tone-change, per-frame scaling and parameter-bound checks pass, as do eight fresh Chrome/WebKit layouts. Complete suite: 78 tests. Whole-window and per-frame resolutions remain distinct in the AI context. Continuous feeds, response correction, phase analysis and the remaining full brief stay open. See docs/SPECTRA.md.


Continuous-feed checkpoint: EarthScope TLS SeedLink acquisition now runs independently of archive retrieval. The ANMO 40 Hz capture retains all 1,745 samples and three original packets with separate source/reception/save times; every packet and sample reproduces exactly. Its source-linked spectrum also reproduces with zero error. A real interrupted connection resumed without a detected data gap or overlap, and worker trees terminate on Stop. Eight Chrome/WebKit layouts, controlled replay/late-response isolation, and both actual brains pass. A local-model acquisition/date error was corrected with focused waveform context and explicit UTC timestamps. The decoder adds pinned ObsPy to the existing isolated Python runtime; dependency checks pass. See docs/INSTRUMENTS.md. Full response correction, phase analysis, broader network and physical-device acceptance, remaining models/geology and the complete visual/distribution brief remain open.

Continuous-feed acceptance: all 79 automated tests pass. Original archive selection/export and both archive AI explanations also pass regression checks. App shutdown was tested while a real 588-sample stream was receiving: no decoder process remained, restart left acquisition stopped, and the saved capture/source hashes remained valid. The forecast ledger remains 282 records with the unchanged valid head; all 417 reviews remain valid. Local Qwen remains the saved default. Shared timestamp formatting now also preserves pre-1970 microseconds.

Response-correction checkpoint: full StationXML response deconvolution now yields saved band-limited displacement, velocity or acceleration estimates with explicit processing and original source provenance. Both the 2,020-sample archive window and 579-sample SeedLink window reproduce every corrected sample and transfer bin exactly. Eight fresh Chrome/WebKit layouts and channel/replay response isolation pass. A numeric passband/transition sentence addresses an observed local-AI filter-description error. See docs/RESPONSE-CORRECTION.md. Joined-packet processing, physical-unit spectral workflows, phase analysis, broader network/device acceptance and the remaining full model/geology/visual/distribution brief remain open.

Response-correction acceptance: complete suite 80 tests; known-gain and worker-cancellation checks pass with no remaining response/SeedLink processes. The original spectrum flow passes eight Chrome/WebKit contexts, and the saved response correction survives restart with all five export integrity checks true. Dependency checks pass. The ledger remains valid at 282 records with unchanged head a66ab0a154d0647fbeb99afb7cc089bf821de8d4f76eecb62d16f68af9e7f81d; all 417 reviews remain valid. Local Qwen remains the default brain.

Joined-waveform checkpoint: archive blocks and stream packets now combine into verified continuous runs for both spectra/time-frequency analysis and response correction. Original sources stay intact; result exports retain exact source slices, all original timestamps, boundary counts and measured clock deviation. Disabling joining reproduces the original block-based calculations. The retained archive uses all 2,400 samples across two blocks (2 µs maximum nominal timing deviation); the SeedLink capture uses all 1,745 samples across three packets (0 µs). Their joined spectrograms contain 17 and 12 complete frames. Both spectra and all corrected values/transfer bins reproduce exactly. Eight fresh Chrome/WebKit layouts pass with 44 px checkbox targets and no overflow. Controlled late calculation, saved selection and AI responses are withheld after joining changes; joining cannot bypass strict replay. Complete suite: 81 tests. Physical-unit spectral workflows, phase analysis and the remaining complete brief stay open. Both actual brains preserve the joining counts and timing; the separate-block spectrum regression passes eight Chrome/WebKit layouts.

Physical-spectrum checkpoint: corrected displacement, velocity and acceleration now feed the existing spectrum/time-frequency tools with squared physical units per hertz. Source switching preserves immutable result identity and withholds delayed responses. Exports retain the spectrum, correction, raw samples and original StationXML, plus the derived timestamp/index mapping to raw blocks. The 2,048-sample ANMO examples yield 1,025 bins and 15 frames for all three quantities, reproduced exactly; six SVG metadata records match JSON. Eight Chrome/WebKit layouts and the original raw-spectrum regression pass. A Windows UTF-8 fix preserves acceleration units. An observed local-AI error about avoiding an original join was corrected with explicit raw-block mapping; Qwen and Astra now report the source chain and physical units accurately. Complete suite: 82 tests. Phase analysis, broader network/device acceptance and the remaining complete scientific/geological/visual/distribution brief remain open. See docs/SPECTRA.md. New corrections are accepted immediately in live mode even when the catalog refresh cutoff is older; the controlled Chrome/WebKit lifecycle check covers this distinction from strict replay.


Prospective-experiment checkpoint: Research now reviews and registers immutable global DS/Recent-rate/Null protocols with frozen configuration, code, sources, schedule and primary comparison. Issuance and fixed-delay assessments run locally within five-minute windows; missing, empty, cancelled and incomplete groups stay visible, with no backdating or replacement by later result reviews. Stop cancels future issuance while existing assessments continue. Exact export reproduction covers targets, controls, outcomes, metrics and primary/secondary contrasts. See docs/PROSPECTIVE-EXPERIMENTS.md.

Prospective acceptance: 87 automated tests pass, including atomic rollback, idempotent registration, stale previews, immutable schedule/results, missed windows, version mismatch, incomplete coverage, origin restrictions and AI source-value guards. Eight fresh Chrome/WebKit desktop/mobile/landscape layouts pass registration, stopping, exports and late-answer isolation. Actual Qwen and Astra preserve the primary result and unavailable state after correcting an app command-matcher leak and adding a targeted guard for Qwen's mistaken removal of a completed score after cancellation. AI prose is not generally fact-validated. All experiments are explicitly synthetic isolated software fixtures; the +100 percentage-point fixture is not scientific forecasting evidence. Main installation preview/registry and restart retain 282 forecasts, 417 valid reviews and unchanged ledger head a66ab0a154d0647fbeb99afb7cc089bf821de8d4f76eecb62d16f68af9e7f81d. No real protocol was registered by acceptance checks. The full scientific, visual, deployment and physical-device goal remains active.


Phase-analysis checkpoint: the instrument workspace now compares a frozen catalog-source hypothesis with IASP91/ak135 theoretical arrivals and saves manual sample picks as immutable child records. Raw and corrected sources preserve separate histories, exact timestamps/units, operator uncertainty, polarity labels, residual signs and full source/parent exports. Short continuous runs reuse the existing timestamp/rate join rules without changing spectral defaults. See docs/PHASE-ANALYSIS.md.

Phase acceptance: 90 automated tests pass. The real Maule/ANMO archive yields six requested branches, with only modeled SS inside the recording interval. Exact reproduction covers raw/corrected outputs, model/source hashes, theoretical timestamps, sample amplitudes, residuals and parent histories. All manual acceptance picks are isolated fixtures; none were inserted into production comparisons. Eight Chrome/WebKit layouts pass after fixing native selector overflow, and both engines preserve editors across resize while rejecting late source/history/AI responses and ineligible strict replay. Actual Qwen/Astra checks preserve counts, acceleration units and the earlier-pick residual interpretation. This implements theoretical comparison and manual annotation, not automated phase detection or source association. The overall goal remains active.

Final phase restart verification retained 282 forecasts, 417 valid result reviews and the unchanged ledger head. Both production phase comparisons contain zero manual picks; the saved annotated examples remain isolated software fixtures. The final phone chart shows the physical amplitude scale, modeled SS marker and exact sample-time origin.

Hemisphere checkpoint: the explicit hemisphere requirement now shares the existing oblique-section renderer and tools, with complementary removed sides, one central clipping plane and a full reference cap. The radial wedge remains available. Both preserve the selected corridor and true depth; schematic layer colors do not represent local geology. Reused camera-following inspection lighting reveals the retained Earth surface in cinematic mode and stays off in scientific mode. See docs/HEMISPHERE-CUTAWAY.md.

Hemisphere acceptance: all 93 tests and eight fresh Chrome/WebKit layouts pass, plus the existing section/picking/dateline/profile regression. The Japan replay preserves 1,507 projected earthquakes and 4,946 Slab2 contour segments across mode/side changes. Exports retain geometry and projection context; a 36-frame Chrome recording stops on a side change and preserves its initial evidence. Actual Qwen and Astra view commands pass after focused presentation context corrected an unrelated forecast answer. The main installation retains 282 valid forecasts, 417 valid reviews, its original ledger head, local Qwen as default and no registered prospective protocols. The full goal remains active.

Trust-maintenance checkpoint: Phone access now shows leaf/issuer/root expiry and prepares a replacement issuer for explicit owner review before activation. The review persists across restart; cancellation removes unused keys. Activation validates the reviewed chain, public download, address and key matches, then atomically selects the generation and clears device credentials. Normal leaf renewal uses the same staged generation approach while retaining phone trust. Retired signing keys are removed after switching; no OS trust is installed. See docs/PHONE-ACCESS.md.

Trust acceptance: all 94 tests and eight fresh Chrome/WebKit layouts pass. Actual isolated TLS checks cover legacy migration, database rollback, altered certificate rejection, old-CA rejection, restart persistence and same-issuer renewal. The browser check also exposed and fixed a first-load Settings race before catalog state was ready. The restarted main app retains its original trust fingerprint 6C969B18700385CF510A279B89ACC6E72BAF2800DD4AD5B9E5206D8868CCAEC2, enabled sharing and no pending replacement. Its existing HTTPS chain validates; 282 forecasts and 417 result reviews remain valid with the unchanged ledger head and local Qwen default. Physical phone trust/firewall/device acceptance, installer/service delivery and the remaining full brief remain open.

User-directed delivery checkpoint (2026-09-19): stopped the packaging detour and removed the unfinished package-builder/installer drafts. Delivered liquid-glass surfaces, brighter cinematic lighting/clouds/atmosphere, a larger 8K desktop globe and streamlined desktop panels. Existing observation data and scientific presentation remain separate. Created C:\Users\blocp\Desktop\SeismoSphere AI.lnk with a globe icon. Launch now starts/reuses the backend, starts installed Ollama when needed, requests catalog refresh and opens a dedicated Edge/Chrome app window. Actual Edge application process verified running.

Delivery acceptance: current USGS month feed loaded (10,838 source events at this check), existing 282-record ledger verified, actual Qwen 3.6 and ChatGPT-authenticated Astra commands both returned successfully. One bounded Chrome check passed desktop, portrait-phone and landscape layouts, event selection and Settings with no JavaScript errors or document overflow. Nine focused TLS/section tests passed after making TLS use the app's own Python with cryptography 50.0.1. Screenshots and reports: artifacts/delivery-*.png, delivery-browser-report.json and delivery-ai.log. This verifies the delivered launch/main-workspace flow; it does not assert that the outstanding full scientific brief or physical-device acceptance has been completed.


Route-network delivery (2026-09-19): implemented explicit directed waypoint graphs and connections, configurable capture distance and bounded downstream traversal, path fulcrums, entered craton-edge progression, along-route spacing targets, swarm redistribution and termination reflection. Engine ds-research-0.3.0 retains existing magnitude modes, silent-zone contrast and 7–10-day windows. New keys include the route revision; issued records retain the full original network. Source-traced routes require URL and map/page/video locator but are never labeled physically validated. SQLite route revisions are append-only, hash-checked and protected against stale writers. Tools → Route network provides waypoint entry/picking, directed globe preview, provenance, history, import/export and save-as-new-version. Prospective snapshots now include the routing implementation source.

Local workspace controls now execute plate-label, magnitude-watch, source-path, fulcrum, scientific/cinematic, cutaway and explicit deep-event commands without AI calls. Filters synchronize cards, globe, frozen visible keys and figure evidence; recordings stop when label/filter context changes. Native reset controls are available. Fixed the corresponding issuance button state and sticky glass-header overlap.

Verification: 98 tests exercised; the first broad run passed 96 and hit two Python startup timeouts. Both timed-out checks subsequently passed in isolation (phase ~3 seconds, response ~3 seconds); npm test now bounds concurrency to two. Focused final engine/routes/prospective/command checks passed 16/16. Actual isolated HTTP/browser saves, source requirements, globe picking, preview, stale-write rejection and original-version preservation passed desktop and phone with 44px controls, no horizontal overflow and no JavaScript errors. Browser acceptance issued no forecasts. The live installation reports engine 0.3.0 and retains the original 282-record ledger hash. Current geometry remains explicitly illustrative. Artifacts: routes-browser-report.json, route-editor-*.png, workspace-commands-report.json.

The final cinematic pass adjusts the illustrative sun direction, ambient light, atmospheric rim and path opacity; desktop/phone renders have no JavaScript errors or horizontal overflow. The user-facing in-app tab was refreshed and switched from its persisted scientific mode to cinematic presentation. Added an archived 0.2 engine for historical randomization reproduction; existing export e9c31c66… replicate 0 reproduces its catalog and forecast digests exactly.
